    # def generate_tasks(self, n=10):
    #     for _ in range(n):
    #         self.create_random_task()

    # def complete_random_task(self):
    #     pending = [t for t in self.tasks if not t.completed]
    #     if pending:
    #         task = random.choice(pending)
    #         task.mark_complete()

    # def show_tasks(self):
    #     for task in self.tasks:
    #         print(task)